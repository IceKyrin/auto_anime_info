# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-python-with-github-actions

name: Python application

on:
  push:
      branches:
        - master
  schedule:
  # 定时任务，在每天的5点
    - cron: '0 21 * * *'

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python 3.10
      uses: actions/setup-python@v3
      with:
        python-version: "3.10"
    - name: 安装模块
      run: |
          pip install --upgrade setuptools==57.5.0
          pip install -r requirements.txt
    - name: update_anime_info
      run: |
        python main.py
      env:
        api_key: ${{secrets.api_key}}
    - name: Commit files
      run: |
        git config --local user.email "996625688+github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git commit -m "Add changes" -a
    - name: Push changes
      run: |
        git remote set-url origin https://GITHUB_TOKEN:${{secrets.git_token}}@github.com/IceKyrin/auto_anime_info.git
        git push -u origin master 
 
